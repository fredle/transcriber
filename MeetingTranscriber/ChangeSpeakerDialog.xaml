<Window x:Class="MeetingTranscriber.ChangeSpeakerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change speaker" Height="400" Width="340" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource WindowBg}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Assign the selected line(s) to:" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <Border Grid.Row="1" Background="{StaticResource Panel}" BorderBrush="{StaticResource Border}"
                BorderThickness="1" CornerRadius="8" Padding="4">
            <ListBox x:Name="OptionsList" SelectionChanged="OnOptionSelected">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DisplayName}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBox x:Name="NewNameBox" Grid.Row="2" Margin="0,10,0,0"
                 Visibility="Collapsed" KeyDown="OnKeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" Width="84" Margin="0,0,8,0" IsCancel="True" Click="OnCancel"/>
            <Button x:Name="OkButton" Content="OK" Width="84" IsDefault="True" IsEnabled="False" Click="OnOk"/>
        </StackPanel>
    </Grid>
</Window>
